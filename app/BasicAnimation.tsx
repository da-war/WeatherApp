import { StyleSheet, Text, View } from "react-native";
import React from "react";

const BasicAnimation = () => {
  return (
    <View>
      <Text>BasicAnimation</Text>
    </View>
  );
};

export default BasicAnimation;

const styles = StyleSheet.create({});
